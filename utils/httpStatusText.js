const SUCCESS = 'success';
const ERROR = 'error';
const FAIL = 'fail';
const UNAUTHORIZED = 'unauthorized';
const FORBIDDEN = 'forbidden';

module.exports = {
    SUCCESS,
    ERROR,
    FAIL,
    UNAUTHORIZED,
    FORBIDDEN,
}